#ifndef PALINDROME_
#define PALINDROME_
#include <stdlib.h>
#include <stdio.h>
int is_palindrome(unsigned long n);
int recursion(unsigned long n, unsigned long t, unsigned long r);
#endif /* PALINDROME_ */
